Image editor module

Photo editing is configured through the interface [[ImageEditorOptions]]

for example:
```js
Zodit.make('#editor', {
  imageeditor: {
    crop: false,
    resize: true,
  }
})
```
